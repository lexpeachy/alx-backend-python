apiVersion: v1
kind: Service
metadata:
  name: django-app-service
spec:
  type: ClusterIP
  selector:
    app: django-app
    version: blue  # Initial traffic to blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
